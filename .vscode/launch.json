{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch SpaceTrouble",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server/main.go",
            "env": {
                "DATABASE_URL": "postgres://postgres:12qw!@QW@localhost/spacetrip_db?sslmode=disable"
            }
        }
    ]
}
